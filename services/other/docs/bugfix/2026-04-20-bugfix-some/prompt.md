# ddo cli客户端修复
1. 添加 /xx-web 系列命令，快速弹出对应功能的web页面
2. 优化cli端添加mcp工具，添加定时任务等操作流程，流程和需要添加的表单和web端保持一致，如：/status-web 用于弹出dashboard页

# 项目图标问题
1. web端的所有项目icon均使用@services\web-ui\public\icons\icon.svg这个图片，包含任务栏托管图标

# go 服务问题修复
1. 现在我创建一个知识库词条，对于一个词条会解析出来多个种类，我希望每次只会解析出来一个大类，防止创建一个词条会在数据库显示多个词条
2. 定时任务执行的时候需要触发web端的灵动岛进行通知

# llm服务回话保存
1. 我现在dashboard需要一个接口用于获取llm调用趋势，我需要在用户本地保存llm的回话记录，这样用户可以随时查看历史记录，并统计回话信息，这部分要好好设计一下，go服务需要提供相应的接口
